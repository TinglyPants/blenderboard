export default function SearchPage({ query }){
    // Gather posts from API using the query. Then display it. Pages wil be handled locally btw.
    // Post components handle the rest!
    // How will i handle 3D models? Maybe rather than a filename, i have an object containing media IDs for the model and its texture files.
    return;
}