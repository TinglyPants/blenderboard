export default function PostCreatePage() {
    // Handle the uploading of data and stuff to the backend.
    // Also make a preview using a modified post component that relies on local data.
    // Maybe work out how to do better local image gathering and stuff in a separate project, boost my understanding kinda thing.
};
