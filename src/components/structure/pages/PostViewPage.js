export default function PostViewPage({ postID }) {
    return;
    // This handles the fullscreen post viewing stuff, with comments and everything.
    // I should have user authorization as a context.
};
